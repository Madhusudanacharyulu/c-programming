#include<stdio.h>
#include<conio.h>
void main(){
	  int n,ele,i,h;
    clrsccr();
	printf("how many numbers?");
  scanf("%d",&n);  // for how many numbers
	i=1;
	while(i<=n){
		scanf("%d",&ele);
		if(i==1)h=ele;
		if(ele>h)
			h=ele;
		i++;
	}
	printf("\nHighest %d",h);
	getch();
}
